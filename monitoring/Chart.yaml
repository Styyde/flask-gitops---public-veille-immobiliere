apiVersion: v2
name: monitoring
description: Wrapper Helm pour kube-prometheus-stack (Prometheus + Grafana), déployé par Argo CD
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: kube-prometheus-stack
    version: "65.5.1"
    repository: https://prometheus-community.github.io/helm-charts
